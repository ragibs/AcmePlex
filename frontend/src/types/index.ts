export interface Movie {
  id: string;
  description: string;
  duration: number;
  genre: string;
  name: string;
  poster: string;
  release_date: string;
}
export interface Theatre {
  id: string;
  name: string;
  address: string;
}
